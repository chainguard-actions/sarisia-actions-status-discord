name: Test payload content
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          status: success
          title: "My Custom Title"
          description: "My Custom Description"
          nofail: "true"
          ack_no_webhook: "true"
          nocontext: "true"
      - name: Assert payload contains description
        env:
          PAYLOAD: ${{ steps.action.outputs.payload }}
        run: |
          echo "payload: $PAYLOAD"
          if [ -z "$PAYLOAD" ]; then
            echo "FAIL: payload is empty"
            exit 1
          fi
          if ! echo "$PAYLOAD" | grep -q "My Custom Description"; then
            echo "FAIL: payload does not contain expected description"
            exit 1
          fi
          echo "PASS"
